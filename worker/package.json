{
  "name": "delovable-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for the Delovable web UI",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development"
  },
  "author": "neckolis",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
